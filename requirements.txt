fastapi==0.104.1
uvicorn[standard]==0.24.0
python-socketio==5.12.0
python-multipart==0.0.6
# redis: removed — never imported; socket.io single-worker on Render free
# tier doesn't need the pub/sub adapter. Re-add as
# `redis==5.x + python-socketio[asyncio_redis]` when we scale to >1 worker.
# Tracked in readmes/DEPLOYMENT.md under the "Scaling" note.
jinja2==3.1.2
aiofiles==23.2.1
# passlib / python-jose removed — no auth code exists. Re-add when
# implementing user accounts or signed-link features.
httpx==0.25.2
pydantic>=2.5.0
beautifulsoup4==4.12.2
cloudscraper==1.2.71

# P2P functionality: libtorrent is NOT installed by default.
#
# The media bridge is WIP and disabled (config.MEDIA_BRIDGE_ENABLED), so the
# dependency is dead weight in a cloud image and an active liability: there is
# no manylinux wheel for every platform, and when pip falls back to building
# against Boost it fails late in the image build. app/services/media_bridge.py
# already degrades gracefully when the import is missing.
#
# To work on the bridge locally:
#   pip install libtorrent==2.0.11   (or: conda install -c conda-forge libtorrent)
#   ENABLE_MEDIA_BRIDGE=true python run.py
google-api-python-client==2.188.0
gunicorn==21.2.0
